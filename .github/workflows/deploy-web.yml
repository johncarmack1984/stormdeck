name: deploy-web

on:
  push:
    branches: [main]
    paths: ['web/**', '.github/workflows/deploy-web.yml']
  workflow_dispatch:

# No AWS keys: the same OIDC role deploy-infra uses, which also carries
# the site-sync grants (s3 site/* + CloudFront invalidation).
permissions:
  id-token: write
  contents: read

concurrency: deploy-web

jobs:
  deploy:
    runs-on: ubuntu-latest
    # Record each publish as a GitHub Deployment to `production`, so the repo's
    # Deployments view tracks what's actually live (with the link) instead of
    # the retired github-pages records. No protection rules — this only logs
    # the deploy, it doesn't gate it.
    environment:
      name: production
      url: https://stormdeck.live
    steps:
      # Full history + tags so the version stamp resolves (see next step).
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      # Bake in the version this commit ships as: its exact tag if it has one,
      # else the patch that auto-release is creating for this same merge — the
      # same number, computed the same way, so the live label is clean and
      # matches the Release (no off-by-one between the deploy and the tag).
      - name: Resolve app version
        run: |
          v=$(git describe --tags --exact-match HEAD 2>/dev/null || scripts/next-version.sh patch)
          echo "VITE_APP_VERSION=$v" >>"$GITHUB_ENV"
          echo "version: $v"
      - uses: pnpm/action-setup@v6
        with:
          version: 10
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
          cache: pnpm
          cache-dependency-path: web/pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
        working-directory: web
      - run: pnpm run build
        working-directory: web
        env:
          # API_BASE resolves to '' in production builds (web/src/config.ts) —
          # same origin as the tiles and weather feeds, so all fetches relative.
          BASE_PATH: /
      - uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ vars.AWS_DEPLOY_ROLE_ARN }}
          aws-region: us-east-2
      - name: Sync site to S3
        run: |
          BUCKET="stormdeck-$(aws sts get-caller-identity --query Account --output text)"
          # Hashed assets are immutable; index.html must always revalidate.
          aws s3 sync web/dist "s3://$BUCKET/site" --delete \
            --exclude index.html \
            --cache-control "public, max-age=31536000, immutable"
          aws s3 cp web/dist/index.html "s3://$BUCKET/site/index.html" \
            --cache-control "no-cache"
      - name: Invalidate index
        run: |
          aws cloudfront create-invalidation \
            --distribution-id ${{ vars.DISTRIBUTION_ID }} \
            --paths "/" "/index.html"
