# deps / build artifacts
node_modules/
dist/
target/
build/

# generated data
*.pmtiles
web/public/weather/

# env / local — dev defaults live in web/src/config.ts; env files are
# personal, machine-local overrides only and are never committed
.env
.env.*
.DS_Store
CLAUDE.md
.claude/
*.tsbuildinfo
