# Local dev: martin runs on :3030 (scripts/dev.sh); weather JSON is served
# by vite from web/public/weather/ (populated by `just weather local`).
VITE_API_BASE=http://localhost:3030
VITE_WEATHER_BASE=
